# Docker
Per-service Dockerfiles go here as needed (backend image, frontend image,
edge-runtime image). The top-level docker-compose.yml currently covers
Postgres/Redis/Chroma using off-the-shelf images.
